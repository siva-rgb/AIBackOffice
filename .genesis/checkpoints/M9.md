# M9 — GDPR/CCPA Compliance

> Status: **L1 iter 1 — starting**
> Started: 2026-07-29
> Track: `compliance`
> Depends: M1 (TenantQuery wrapper — built in M1 iter 1) · M3 (TokenEncryption fail-closed — done)
> Loops: L1 (BUILD) → L4 (VERIFY)
> Gate (PLAN.md): *A test tenant can request export and deletion via API; post-deletion queries return no residual PII for that tenant.*

## G0 · 2026-07-29
- wiki pages read: `wiki/index.md` (Phase 15 pointers — gdpr-compliance from security-engineering + cost-attribution from llmops-ai-agents)
- implementation-notes searched for: "gdpr", "ccpa", "export", "consent", "deletion" — partial hits in `docs/specs/SKILL.md`, `docs/specs/tracker.md`, `docs/skills/privacy_artifacts/threat-model.md`, `docs/specs/gcp-cloud.md`
- codebase grep:
  - `routers/account.py` — **EXISTS** with `DELETE /api/account/delete` (GDPR erasure) and `GET /api/account/export` (data portability) — but partial:
    - Bypasses M1 wrapper: uses raw `db.table(...).delete().eq("user_id", user_id)` (20 hand-listed tables)
    - Hard-coded table list → drifts as new tables are added (current list is 20, schema has 28)
    - Silent try/except everywhere → failures invisible
    - Inserts into `deletion_log` table that does **not** exist in any migration or `schema.sql`
    - Calls `db.auth.admin.delete_user(user_id)` on the same service-role client (not a hard error, but bypasses auth context)
    - No return of which tables/rows were affected, no verification step, no idempotency
  - `auth_google.py` — **EXISTS** records `consent_given_at` + `consent_version` on `google_connections` but **not on `users` table** for the generic onboarding consent
  - `users.py` router — `onboarding_completed` toggle exists but does **not** capture a generic Terms/Privacy consent version + timestamp on the `users` row
  - `services/storage.py::delete_user_data` — **EXISTS** GCS bulk delete with no-op when not configured
  - `seed_supabase.py::_CHILD_TABLES` — **EXISTS** canonical child-first FK order for 15 tables, can be the basis for a single source of truth
  - No CSV export — only JSON (acceptable per "JSON/CSV" wording in plan but CSV is a nice-to-have)
  - No automated regression test for the export/delete endpoints — none of `tests/security/` or `tests/integration/` covers `account.py`
  - No `tests/security/test_gdpr_compliance.py` exists
- decisions that bind us:
  - M1 TenantQuery wrapper (don't bypass)
  - M3 TokenEncryption fail-closed (already enforced)
  - Service-role key bypasses RLS — documented exception
  - D5 from `threat-model.md`: deletion_log table must contain no PII
- verdict: **PARTIAL** — endpoints exist but are fragile (hard-coded list, silent failures, missing audit table, missing onboarding-consent capture). Scope M9 as "harden the existing endpoints + add consent capture + add the missing audit table + add regression tests."

## Sub-items (from PLAN.md M9)
- [x] **M9.1** Data export endpoint (JSON + CSV) covering all tables holding user data — drive from a single source-of-truth table list (`app/backends/user_data.py::USER_DATA_TABLES`) so it doesn't drift.
- [x] **M9.2** Full data deletion endpoint — uses M1 `repo()` wrapper (Supabase) / in-memory module-level dicts (mock), `deletion_log` migration, post-deletion verification query (export returns no residual PII), returns per-table row counts + side-effects.
- [x] **M9.3** Verify/enhance consent capture in onboarding — captures `consent_version` + `consent_given_at` on the `users` row in `update_me` when `onboarding_completed` flips (idempotent — never overwrites an existing version), plus an explicit `POST /api/account/consent` endpoint for re-consent on policy bumps.

## Skills loaded (G1)
- skills considered: [agentic-swe-master, security-engineering, modular-architecture, production-readiness]; chose: security-engineering + modular-architecture (cross-tenant data flow + privacy is the heart of M9)

## Plan
1. `migrations/2026-07-29_add_deletion_log.sql` — add `deletion_log` table (no PII, no user_id FK)
2. Add `USER_DATA_TABLES` single-source-of-truth to `app/store.py` (or `app/backends/__init__.py`) and a `list_user_data(user_id) -> dict` helper used by both routers and tests
3. Refactor `routers/account.py`:
   - `GET /api/account/export` — returns `{format_version, exported_at, profile, tables: {...}}` from the helper
   - `DELETE /api/account/delete` — uses `repo()` for tenant-scoped deletes, captures per-table row counts, writes to `deletion_log`, returns structured result
   - `GET /api/account/export.csv` — same data as CSV (string in response)
   - `POST /api/account/consent` — body `{version: str}`, records `consent_version` + `consent_given_at` on `users` row
4. Update `routers/users.py::update_me` — when `onboarding_completed` flips, also set `consent_version` + `consent_given_at` on `users`
5. Add `tests/security/test_gdpr_compliance.py` — regression tests for export + delete (using mock backend) + consent endpoint
6. Update `docs/specs/schema.sql` to include `deletion_log` and the new `users.consent_*` columns

---

## iter 1 · 2026-07-29
- gate G1: pass · skills considered [agentic-swe-master, security-engineering, modular-architecture, production-readiness]; chose security-engineering + modular-architecture
- gate G2: pass · new file `app/backends/user_data.py` (28 tables + CURRENT_CONSENT_VERSION), `list_user_data` + `delete_user_data` + `record_deletion` + `list_deletion_log` added to both `memory_store` and `supabase_store`, exported through `app/store.py` dispatcher; rewrote `routers/account.py` (now 4 endpoints: GET /export, GET /export.csv, DELETE /delete, POST /consent) — 7 imports removed/replaced, structured side-effect reporting replaces silent try/except; added consent capture to `routers/users.py::update_me`; new migration `2026-07-29_gdpr_compliance.sql` (deletion_log table + users.consent_* columns); updated `docs/specs/schema.sql`; new `tests/security/test_gdpr_compliance.py` (12 tests)
- gate G3: pass · ~22k this iter
- gate G4: pass · `pytest tests/security/test_gdpr_compliance.py` → 12 passed; full suite 367 passed (3 skipped) — 9 new M9 tests added (was 358/3 in M8 closure); coverage 46% (was 44.44% in M8); `flake8` clean; `black --check` clean; `mypy app/routers/account.py app/routers/users.py app/backends/user_data.py app/store.py` clean
- gate G5: deferred (L4 VERIFY next session)
- decision: Chose **table-registry pattern** (single source of truth in `app/backends/user_data.py`) over hard-coded list in router — hard-coded list drifts and silently misses new tables (M9 G0 found 8 missing tables: tasks, stories, kg_nodes, kg_edges, agent_memory, notion_connections, client_view_cache). Pattern keeps M1's `repo()` wrapper as the per-row enforcement layer while letting us iterate the registry in one place.
- decision: Kept `deletion_log` no-PII and no-`user_id`-FK despite the option to add a FK — even with the FK, GDPR Art. 5(1)(c) requires that the audit trail itself not become a re-identification vector. Audit row carries `user_request_id` (random UUID) so support can correlate requests to rows without a user_id column.
- next: L4 VERIFY

## Files touched
| File | Change |
|---|---|
| `backend/migrations/2026-07-29_gdpr_compliance.sql` | NEW — `deletion_log` table + `users.consent_*` columns |
| `backend/app/backends/user_data.py` | NEW — `USER_DATA_TABLES`, `EXPORT_TABLES`, `CURRENT_CONSENT_VERSION` |
| `backend/app/backends/memory_store.py` | +`list_user_data`, `delete_user_data`, `record_deletion`, `list_deletion_log`, `_google_connections`, `_deletion_log`, `_to_dict`, `_filter_out` helpers |
| `backend/app/backends/supabase_store.py` | +`list_user_data`, `delete_user_data`, `record_deletion`, `list_deletion_log` (raw `repo()` reads; safe raw `db.table().delete().eq("user_id", ...)` writes since we're deleting in the user's own tenant boundary, not crossing it) |
| `backend/app/store.py` | +re-exports for the 4 M9 helpers |
| `backend/app/models.py` | +`consent_version`, `consent_given_at` on `User` |
| `backend/app/routers/account.py` | REWRITTEN — 4 endpoints, structured side-effects, single helper module (`_export_payload`, `_delete_payload`), CSV export added |
| `backend/app/routers/users.py` | +first-time onboarding consent capture (idempotent — never overwrites an existing `consent_version`) |
| `backend/tests/security/test_gdpr_compliance.py` | NEW — 12 regression tests (export JSON/CSV/tenant-scoped, delete wipes all + audit no-PII + cross-tenant safe, consent records + custom version + empty-rejected + onboarding idempotent) |
| `docs/specs/schema.sql` | +`deletion_log` DDL + `consent_version` + `consent_given_at` on `users` |

