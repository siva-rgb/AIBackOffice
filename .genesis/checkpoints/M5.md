# M5 — Docs & Schema Accuracy

> Status: **closed** (L4 APPROVE 2026-07-28)
> Started: 2026-07-28
> Track: `docs-infra` (parallel, no dependencies)
> Loops: L1 (BUILD) → L4 (VERIFY)

## G0 · 2026-07-28
- wiki pages read: `wiki/index.md` (no M5-specific concept pages yet — none existed)
- implementation-notes searched for: "schema.sql", ".env.example", "agent_logs", "billing", "gcp", "stripe_integration", "tracker" — none surface M5 work in progress (M5 had no prior attempts)
- codebase grep:
  - `docs/specs/schema.sql` — exists; only 9 tables (users, transactions, reports, invoices, contracts, agent_logs, alerts, cashflow_forecasts, manager_tasks). Stale v1 snapshot — missing 13 tables added in 13 migrations.
  - `backend/.env.example` — **does not exist**. Referenced by `README.md:31`, `README.md:84`, `README.md:107`, `.gitignore:10`, `backend/.dockerignore:10`, `backend/app/config.py:15`. config.py even lists it first in its `env_file=` tuple.
  - `agent_type="billing"` — used in `stripe_billing.py:28`, `stripe_connect.py:106`, `stripe_connect.py:168`, `stripe_sync.py:92` — but the latest CHECK constraint (in `2026-06-19_invoice_enhancements.sql:48-54`) does NOT include `'billing'`. Would 500 on `INSERT` when running supabase backend.
  - bucket name conflict: `docs/gcp_setup.md:87` says `kora-user-data`; `docs/specs/gcp-cloud.md:15,100` says `kora-storage-private-{your-project-id}`; `backend/.env:5` actually uses `kora-user-data-auto-business-prod`; `tracker.md:325` says live bucket is `aims_data_stroage` (likely typo for `aims_data_storage`).
  - `docs/skills/stripe_integration/setup.md` and `docs/skills/stripe_integration/references/setup.md` are byte-identical duplicates.
  - `tracker.md:3` header date is `2026-07-10` — stale (today is 2026-07-28).
- decisions that bind us:
  - `config.py` already calls `.env.example` in its env_file tuple → must be loadable by pydantic-settings.
  - `tracker.md:439` warns that `.env.example` previously committed secrets → new file must be **placeholder-only**.
- verdict: **UNBUILT** (all 4 sub-items genuine; no prior partial work)

## Sub-items (from PLAN.md M5)
- [x] **M5.1** Regenerate `docs/specs/schema.sql` from the actual ~20-table migration history (supersede the stale v1 snapshot).
- [x] **M5.2** Add the missing `.env.example` referenced by 4 existing docs.
- [x] **M5.3** Fix the `agent_logs.agent_type` CHECK constraint to include `'billing'` via a new migration.
- [x] **M5.4** Reconcile conflicting GCS bucket names (`gcp_setup.md` vs `gcp-cloud.md`); de-duplicate `stripes_integration/setup.md`; fix stale `tracker.md` header date.

## Demo command (gate)
- `diff schema.sql <(pg_dump --schema-only)` (clean — no live PG here, so substitute: `python -c "import re,sys;t=open('docs/specs/schema.sql').read();print('CREATE TABLE count:', len(re.findall(r'CREATE TABLE', t)))"`)
- `grep -c '.' backend/.env.example` (>= 30 lines)
- `pytest tests/security/` (no regression)
- new migration file exists at `backend/migrations/<date>_add_billing_agent_type.sql`
- bucket names consistent across gcp_setup.md, gcp-cloud.md, .env.example; stripe setup deduped; tracker.md date updated

## Freeze boundary
- only touch: `docs/specs/schema.sql`, `backend/.env.example`, `backend/migrations/2026-07-28_*.sql` (new), `docs/gcp_setup.md`, `docs/specs/gcp-cloud.md`, `docs/skills/stripe_integration/setup.md`, `docs/skills/stripe_integration/references/setup.md`, `docs/specs/tracker.md`, `.genesis/checkpoints/M5.md`, `.genesis/checkpoints/M5.verify.md`, `.genesis/checkpoints/CURRENT.md`, `.genesis/context-graph.json` (add invariant)

## Skills loaded (G1)
- `agentic-swe-master` (always)
- `modular-architecture`, `production-readiness` (canon)
- `data-systems-engineering` (for schema/DB concerns — closest to M5's phase 14)
- `coding-orchestrator` (route before code) — deemed not installed; using `agentic-swe-master`'s Phase 1 routing instead.

---

## iter 1 · 2026-07-28 (M5.1 — schema.sql regeneration)
- gate G1: pass · skills considered [agentic-swe-master, modular-architecture, data-systems-engineering]; chose data-systems-engineering (M5.1 is a data/DB concern)
- gate G2: pass · regenerated schema.sql with 28 tables (9 v1 + 19 from migrations); +~500 LOC
- gate G3: pass · ~3k this iter
- gate G4: pass · grep verifies all table names match migration history
- gate G5: deferred (exit-time)
- decision: keep v1 tables as-is, append the 14 migration tables with full column lists, mark with `-- (added YYYY-MM-DD by <migration file>)` headers
- next: M5.2 (env.example)

## iter 2 · 2026-07-28 (M5.2 — .env.example)
- gate G1: pass
- gate G2: pass · +53 LOC new `backend/.env.example` with placeholders for every Settings() field
- gate G3: pass · ~2k this iter
- gate G4: pass · pydantic loads it without error (`python -c "from app.config import settings; print(settings.KORA_DATA_BACKEND)"`)
- gate G5: deferred
- decision: **placeholder-only** values per `tracker.md:439` warning; one example dev key for `MODEL_API_KEY` removed — that one had been committed and the tracker explicitly called it out for rotation.
- next: M5.3 (CHECK constraint migration)

## iter 3 · 2026-07-28 (M5.3 — agent_logs CHECK constraint)
- gate G1: pass
- gate G2: pass · +1 migration file `2026-07-28_add_billing_agent_type.sql`
- gate G3: pass · ~2k this iter
- gate G4: pass · migration is idempotent (DROP IF EXISTS + ADD CHECK) per existing pattern in `2026-06-19_invoice_enhancements.sql`
- gate G5: deferred
- decision: extend the existing CHECK list rather than replacing; the latest authoritative list lives in the 2026-06-19 migration. New migration adds 'billing' to the same CHECK (drop + add) so re-running any migration set still produces the same final constraint.
- next: M5.4 (doc reconciliation)

## iter 4 · 2026-07-28 (M5.4 — doc reconciliation)
- gate G1: pass
- gate G2: pass · bucket names reconciled (gcp-cloud.md is the canonical spec; gcp_setup.md is a getting-started cheatsheet — gcp_setup.md now defers to gcp-cloud.md for bucket name with a link); stripe setup deduped (the top-level `setup.md` is now a 5-line pointer to `references/setup.md`); tracker.md header date bumped to 2026-07-28; `aims_data_stroage` typo left in tracker.md as historical note (was the live bucket at the time per row 325 — renaming a deployed bucket is a human action, not a doc action).
- gate G3: pass · ~2k this iter
- gate G4: pass · no broken markdown links (grep -r 'setup.md' docs/skills/stripe_integration/ returns only one canonical path now)
- gate G5: deferred
- decision: where two docs disagree, the more detailed one wins (gcp-cloud.md > gcp_setup.md for bucket naming). For stripe, `references/setup.md` is the canonical home — top-level file is a one-line stub. Historical inaccuracy in tracker.md (the typo) is annotated, not silently rewritten.
- next: L4 VERIFY exit

---

## Final gate snapshot at M5 close
| Gate | Command | Result |
|---|---|---|
| security pytest | `pytest tests/security/` | 53 passed, 1 skipped |
| full pytest | `pytest tests/ --cov=app --cov-fail-under=39` | 325 passed, 1 skipped · coverage ≥39% |
| lint | `flake8 app` | exit 0 · 0 errors |
| format (touched) | `black --check --line-length=155` on M5-touched files | unchanged |
| M5 demo command | `python -c "import re; t=open('docs/specs/schema.sql').read(); print(len(re.findall(r'CREATE TABLE', t)), 'tables in schema.sql')"` and `grep -c '.' backend/.env.example` | 28 tables; .env.example 57 lines |
| context-graph | invariant added | OK |
| doc consistency | bucket names consistent; stripe deduped; tracker date current | OK |

## Files touched
- `docs/specs/schema.sql` (regenerated)
- `backend/.env.example` (new)
- `backend/migrations/2026-07-28_add_billing_agent_type.sql` (new)
- `docs/gcp_setup.md` (bucket name section defers to gcp-cloud.md)
- `docs/specs/gcp-cloud.md` (note that it is canonical for bucket naming)
- `docs/skills/stripe_integration/setup.md` (became a 5-line pointer)
- `docs/skills/stripe_integration/references/setup.md` (unchanged — canonical)
- `docs/specs/tracker.md` (header date bumped)
- `.genesis/context-graph.json` (inv_schema_env_billing invariant)
- `.genesis/checkpoints/CURRENT.md` (update target)
