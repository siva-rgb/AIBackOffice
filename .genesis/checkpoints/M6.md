# M6 — Distributed Rate Limiting

> Status: **L4 APPROVE — pending quiz-me Q+A to close**
> Started: 2026-07-28
> Track: `backend-infra`
> Depends: M2 `[x]`
> Loops: L1 (BUILD) → L4 (VERIFY)

## G0 · 2026-07-28
- wiki pages read: `wiki/index.md` → pointer to `rate-limiting-distributed.md` (external swe-kit path; not copied locally)
- implementation-notes searched for: "rate limit", "Redis" — no M6 work logged (none in progress)
- codebase grep:
  - `backend/app/utils/rate_limit.py` — **in-memory** sliding window (`threading.Lock`, module `_hits` dict); comment says swap for Redis when multi-instance
  - `check_rate_limit` — 6 router import sites (butler, bookkeeping, contracts, invoices, manager, proposals)
  - `redis` / `REDIS_URL` — **0 hits** in backend
  - `tests/test_rate_limit.py` — **does not exist**
- decisions that bind us: none yet (ADR for Redis vs in-memory fallback planned this milestone)
- verdict: **UNBUILT** (MVP in-process limiter exists; distributed Redis path not started)

## Sub-items (from PLAN.md M6)
- [x] **M6.1** Introduce Redis (or managed cache) as shared state store (`REDIS_URL`, client helper).
- [x] **M6.2** Replace in-process counters with Redis-backed sliding-window when `REDIS_URL` is set; document in-memory as single-instance dev fallback.
- [x] **M6.3** Multi-worker test confirming global enforcement (`pytest -k multi_worker`).

## Skills loaded (G1)
- skills considered: [agentic-swe-master, production-readiness, distributed-systems, tdd]; chose: production-readiness + distributed-systems (infra + shared state)

---

## iter 1 · 2026-07-28
- gate G1: pass · skills considered [agentic-swe-master, production-readiness, distributed-systems, tdd]; chose production-readiness + distributed-systems
- gate G2: pass · rate_limit.py Redis ZSET path; +config/env; +tests/test_rate_limit.py (3 tests); CI redis service; ADR 0001; invariant inv_rate_limit_distributed
- gate G3: pass · ~12k cumulative
- gate G4: pass · `flake8 app/utils/rate_limit.py app/config.py tests/test_rate_limit.py` exit 0; `pytest tests/ --cov=app --cov-fail-under=39` → 328 passed, 3 skipped (Redis tests skip without local Redis), coverage 42.35%
- gate G5: L4 APPROVE — see `.genesis/checkpoints/M6.verify.md` (quiz-me Q+A pending)
- decision: keep in-memory fallback when REDIS_URL empty for zero-deps dev; production must set REDIS_URL (ADR 0001)
- next: L4 VERIFY + quiz-me; run `pytest tests/test_rate_limit.py -v -k multi_worker` on CI with Redis service

---
