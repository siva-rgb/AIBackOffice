# M1 — Tenant Isolation Enforcement

## G0 · 2026-07-27
- wiki pages read: (none — initial M1)
- implementation-notes searched for: "tenant isolation", "repository", "token_encryption" — partial
- codebase grep: `_sb.table(` — 71 hits in supabase_store.py; `tests/security/` directory empty; `repository.py` existed but unused
- decisions that bind us: service-role key bypasses RLS (documented exception)
- verdict: PARTIAL — foundation exists (71 .eq("user_id",...) calls, mock store enforces isolation, auth dependency wired), but missing: automation layer, insert-time validation, tests, token fail-closed

## iter 1 · 2026-07-27
- gate G1: pass · skills: agentic-swe-master, security-engineering (per DONE.html M1-M5 section)
- gate G2: pass · M1.1 integrated repository layer (repo() wrapper) across all ~65 Supabase query functions in supabase_store.py; M1.2 confirmed zero direct _sb.table() bypass in routers/services; M1.3 RLS migration created + token_encryption fail-closed; M1.4 24 regression tests pass
- gate G3: pass · tokens within budget
- gate G4: pass · `pytest tests/security/` → 24 passed, 1 skipped; existing store/nocion tests → 26 passed (0 regressions)
- gate G5: deferred (L4 VERIFY next session)
- decision: Kept 9 _sb.table() calls for user auth/memory/agent-logs-list-all — all are justified (tenant identity lookup, cross-tenant admin dashboard). The `repo()` wrapper uses `select`, `insert`/`_check_user_id`, `update`(id+user_id), `delete`(id+user_id), `upsert`(on_conflict), and `raw_table()` for batch/pre-existing-query cases.
- next: L4 VERIFY from separate model context